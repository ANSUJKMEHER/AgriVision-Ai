# Copy this file to `.env` and adjust as needed.
# The React app reads this at build/dev time via import.meta.env.

# URL of the FastAPI inference backend (default: http://localhost:8000)
VITE_API_URL=http://localhost:8000
